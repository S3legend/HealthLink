spring.application.name=login
# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/login_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate (JPA) Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

spring.security.oauth2.client.registration.google.client-id=YOUR_GOOGLE_CLIENT_ID
spring.security.oauth2.client.registration.google.client-secret=YOUR_GOOGLE_CLIENT_SECRET
spring.security.oauth2.client.registration.google.scope=profile,email

spring.mail.host=smtp.gmail.com               # Serveur SMTP de Gmail
int spring.mail.port=587                           # Port SMTP sécurisé
spring.mail.username=fuorzajasmin02@gmail.com      # Remplacez par votre adresse Gmail
spring.mail.password=yeeeey123           # Remplacez par le mot de passe de votre compte
spring.mail.properties.mail.smtp.auth=true     # Active l'authentification SMTP
spring.mail.properties.mail.smtp.starttls.enable=true  # Active la sécurité STARTTLS
